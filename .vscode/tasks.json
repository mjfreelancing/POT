{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "docker-start-pot",
      "type": "shell",
      "command": "docker-compose -p pot -f docker-compose.yml up --build -d",
      "options": {
        "cwd": "${workspaceFolder}/Source/Docker"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": []
    },
    {
      "label": "docker-stop-pot",
      "type": "shell",
      "command": "docker-compose -p pot -f docker-compose.yml down",
      "options": {
        "cwd": "${workspaceFolder}/Source/Docker"
      },
      "group": {
        "kind": "build",
        "isDefault": false
      },
      "problemMatcher": []
    }
  ]
}
