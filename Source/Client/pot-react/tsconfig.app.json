{
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.app.tsbuildinfo",
    // ES2022 target/lib required by react-router v8 (its types + shipped lib target)
    "target": "ES2022",
    "useDefineForClassFields": true,
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,

    // Refer to: https://ui.shadcn.com/docs/installation/vite
    "paths": {
      "@/*": ["./src/*"],
      "@tests/*": ["./tests/*"]
    },

    /* Bundler mode */
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "isolatedModules": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "react-jsx",

    /* Linting */
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "noUncheckedSideEffectImports": true,
    // 'node' is included because src/lib/utils.ts reads process.env.NODE_ENV and
    // the vitest suites (tsconfig.app includes tests/) set it; the browser bundle
    // itself has process.env.NODE_ENV replaced by Vite at build time.
    "types": ["vite-plugin-pwa/client", "node"]
  },
  "include": ["src", "tests"]
}
