# version: "3.8"
services:
  postgres:
    container_name: pot-postgres
    build:
      context: Postgres
      dockerfile: Dockerfile
    restart: always
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
    ports:
      - "5444:5432"
    volumes:
      - ./postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - pot-network

  server:
    container_name: pot-aspnet
    build:
      # The context needs to be up one level so the server source code can be found
      context: ..
      dockerfile: Docker/Server/Dockerfile
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - DATABASE__HOST=pot-postgres
      - DATABASE__USERNAME=${POSTGRES_USER}
      - DATABASE__PASSWORD=${POSTGRES_PASSWORD}
      - RSA__PRIVATEKEY=${RSA_PRIVATE_KEY}
      - ASPNETCORE_URLS=http://+:5241
    ports:
      - "5241:5241"
    healthcheck:
      test: "curl -f http://localhost:5241 || exit 1"
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - pot-network

networks:
  pot-network:
    driver: bridge
